<script lang="ts">
	import { base } from '$app/paths';
</script>

<!-- Reached only if the load-function redirect is bypassed (e.g. JS disabled). -->
<p>Redirecting to the <a href="{base}/voltage-divider">voltage divider</a>…</p>
