import raw from '@go2dev/wt-theme/tokens.json';

export type WtThemeTokens = typeof raw;

/** Whatever Together design tokens (from `@go2dev/wt-theme`; edit upstream in go2dev/whatever-together). */
export const wtThemeTokens: WtThemeTokens = raw;

export default raw;
